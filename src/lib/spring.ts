"use client";

export const SPRING_GENTLE = { type: "spring" as const, stiffness: 120, damping: 14 };
export const SPRING_SNAPPY = { type: "spring" as const, stiffness: 400, damping: 30 };
export const SPRING_BOUNCY = { type: "spring" as const, stiffness: 300, damping: 20 };
export const SPRING_SLOW = { type: "spring" as const, stiffness: 80, damping: 20 };
export const SPRING_CURSOR = { type: "spring" as const, stiffness: 150, damping: 15 };
